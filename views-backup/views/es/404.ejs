<%- include('../partials/head', { lang, t, alternates }) %>
<%- include('../partials/navbar', { lang, t }) %>

<section class="pt-28 pb-20 px-4 sm:px-6 min-h-screen flex items-center justify-center">
  <div class="text-center animate-fade-in">
    <div class="xs-card p-10 inline-block">
      <h1 class="text-7xl font-black text-pinky-400 tracking-tight">404</h1>
      <p class="text-gray-500 mt-4">Página no encontrada</p>
      <a href="<%= lang === 'en' ? '' : '/' + lang %>/" class="xs-btn xs-btn-primary mt-6">Ir al inicio</a>
    </div>
  </div>
</section>

<%- include('../partials/footer', { lang, t }) %>
