<%- include('../../partials/head', { lang, t, alternates }) %>
<%- include('../../partials/navbar', { lang, t }) %>

<section class="pt-28 pb-20 px-4 sm:px-6 min-h-screen">
  <div class="max-w-2xl mx-auto">
    <h1 class="text-3xl sm:text-4xl font-black text-center mb-2 animate-fade-in">Validar Certificate</h1>
    <p class="text-gray-500 text-center mb-8 text-sm font-light animate-slide-up">Insira um codigo de certificado para verificar sua autenticidade.</p>

    <div class="xs-card xs-card-glass p-6 animate-fade-in">
      <div class="flex gap-3 mb-6">
        <input id="certCode" type="text" class="xs-input flex-1 font-mono" placeholder="e.g. XS-A1B2C3D4-E5F6" maxlength="20">
        <button id="ValidarBtn" class="xs-btn xs-btn-primary shrink-0">Validar</button>
      </div>
      <div id="validationResult" class="hidden"></div>
    </div>

    <% if (result) { %>
      <div class="xs-card xs-card-glass p-6 mt-6 <%= result.valid ? 'border-green-200' : 'border-red-200' %>">
        <% if (result.valid) { %>
          <div class="text-center">
            <svg class="w-12 h-12 text-green-500 mx-auto mb-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
            <h2 class="text-xl font-black text-green-700 mb-1">Certificado Valido</h2>
            <p class="text-sm text-green-600 mb-4">This certificate has been verified as authentic.</p>
            <div class="bg-green-50 rounded-xl p-4 text-left text-sm space-y-2">
              <div><span class="font-semibold text-gray-600">Code:</span> <code class="font-mono"><%= result.certificate.code %></code></div>
              <div><span class="font-semibold text-gray-600">Recipient:</span> <%= result.certificate.userName %></div>
              <div><span class="font-semibold text-gray-600">Course:</span> <%= result.certificate.courseName %></div>
              <div><span class="font-semibold text-gray-600">Emitidos:</span> <%= new Date(result.certificate.EmitidosAt).toLocaleDateString() %></div>
              <div><span class="font-semibold text-gray-600">Points:</span> <%= result.certificate.points %></div>
            </div>
          </div>
        <% } else { %>
          <div class="text-center">
            <svg class="w-12 h-12 text-red-400 mx-auto mb-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/></svg>
            <h2 class="text-xl font-black text-red-500 mb-1">Certificado Invalido</h2>
            <p class="text-sm text-red-400"><%= result.error || 'Code not found or certificate not yet Emitidos.' %></p>
          </div>
        <% } %>
      </div>
    <% } %>
  </div>
</section>

<script>
(function() {
  var ValidarBtn = document.getElementById('ValidarBtn');
  var certCode = document.getElementById('certCode');
  var resultDiv = document.getElementById('validationResult');

  if (ValidarBtn) {
    ValidarBtn.addEventListener('click', function() {
      var code = certCode.value.trim();
      if (!code) { alert('Enter a certificate code.'); return; }
      resultDiv.classList.add('hidden');

      fetch('/api/courses/certificates/Validar/' + encodeURIComponent(code))
        .then(function(r) { return r.json(); })
        .then(function(d) {
          if (d.ok && d.valid) {
            resultDiv.innerHTML = '<div class="bg-green-50 border border-green-200 rounded-xl p-5 text-center">'
              + '<svg class="w-10 h-10 text-green-500 mx-auto mb-2" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>'
              + '<h3 class="font-bold text-green-700 mb-1">Certificado Valido</h3>'
              + '<p class="text-sm text-green-600">Emitidos to <strong>' + escapeHtml(d.certificate.userName) + '</strong> for <strong>' + escapeHtml(d.certificate.courseName) + '</strong></p>'
              + '<p class="text-xs text-gray-500 mt-1">Code: <code class="font-mono">' + escapeHtml(d.certificate.code) + '</code></p>'
              + '</div>';
          } else {
            resultDiv.innerHTML = '<div class="bg-red-50 border border-red-200 rounded-xl p-5 text-center">'
              + '<svg class="w-10 h-10 text-red-400 mx-auto mb-2" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/></svg>'
              + '<h3 class="font-bold text-red-500 mb-1">Certificado Invalido</h3>'
              + '<p class="text-sm text-red-400">' + escapeHtml(d.error || 'Code not found or not yet Emitidos.') + '</p>'
              + '</div>';
          }
          resultDiv.classList.remove('hidden');
        })
        .catch(function() {
          resultDiv.innerHTML = '<div class="bg-red-50 border border-red-200 rounded-xl p-5 text-center"><p class="text-red-500 text-sm">Network error. Please try again.</p></div>';
          resultDiv.classList.remove('hidden');
        });
    });
  }

  certCode.addEventListener('keydown', function(e) {
    if (e.key === 'Enter') { ValidarBtn.click(); }
  });

  function escapeHtml(str) {
    if (!str) return '';
    var div = document.createElement('div');
    div.textContent = str;
    return div.innerHTML;
  }
})();
</script>

<%- include('../../partials/footer', { lang, t }) %>
