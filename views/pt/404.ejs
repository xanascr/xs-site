<%- include('../partials/head', { lang, t, alternates }) %>
<%- include('../partials/navbar', { lang, t }) %>

<section class="pt-28 pb-20 px-4 sm:px-6 min-h-screen flex items-center justify-center">
  <div class="text-center">
    <h1 class="text-6xl font-black text-pinky-400">404</h1>
    <p class="text-gray-500 mt-4 text-lg">Página não encontrada</p>
    <a href="/<%= lang === 'en' ? '' : lang %>/" class="mt-6 inline-block text-pinky-400 hover:underline">Voltar ao início</a>
  </div>
</section>

<%- include('../partials/footer', { lang, t }) %>
