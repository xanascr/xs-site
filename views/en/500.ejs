<%- include('../partials/head', { lang, t, alternates }) %>
<%- include('../partials/navbar', { lang, t }) %>

<section class="pt-28 pb-20 px-4 sm:px-6 min-h-screen flex items-center justify-center">
  <div class="text-center max-w-md animate-fade-in">
    <div class="text-6xl font-black text-gray-200 mb-4">500</div>
    <h1 class="text-2xl font-bold mb-2">Internal Server Error</h1>
    <p class="text-gray-500 text-sm mb-6">Something went wrong. Please try again later.</p>
    <a href="/<%= lang %>" class="xs-btn xs-btn-primary">Go Home</a>
  </div>
</section>

<%- include('../partials/footer', { lang, t }) %>